#!/usr/bin/env node

/**
 * Author: Dom Armstrong, Date: 18/05/15
 *
 * Start the server. If this project was installed from npm the server would be started by:
 * ```
 * require('linkedIn-cv').init();
 * ```
 */

require('./').init();